# 🚀 TeamVConnect — قائمة التحسينات المقترحة (Roadmap)

هذه الوثيقة تحتوي على خارطة الطريق والميزات الإضافية التي ستحول التطبيق من مجرد "مشاركة شاشة" إلى "برنامج تحكم عن بُعد احترافي" (Remote Desktop) متكامل مثل TeamViewer.

---

## 1. 🖱️ التحكم الفعلي في الماوس ولوحة المفاتيح (أولوية قصوى)
*   **الوصف:** حالياً، التطبيق يقوم بجمع إحداثيات المشغل وإرسالها عبر قناة البيانات (DataChannel)، لكن الجهاز المضيف لا ينفذها.
*   **التنفيذ:** 
    *   استخدام مكتبة مثل `@nut-tree/nut-js` أو `robotjs` (للـ Node.js) في الـ Agent الخاص بالمضيف.
    *   ترجمة الإحداثيات المستلمة (X, Y) من الشاشة المشتركة إلى ضغطات فعلية للماوس والكيبورد في نظام تشغيل المضيف.
*   **الهدف:** السماح للعميل بالنقر، السحب، والكتابة الفعلية على جهاز المضيف.

## 2. 🔐 نظام الموافقة (Accept/Reject) والأمان الإضافي
*   **الوصف:** إضافة طبقة حماية إضافية قبل السماح بمشاركة الشاشة والتحكم.
*   **التنفيذ:**
    *   عندما يُدخل العميل الكود الصحيح، تظهر نافذة منبثقة للمضيف (Prompt).
    *   النافذة تحتوي على طلب الموافقة أو الرفض لمنح العميل إذن الاتصال.
    *   إضافة ميزة "كلمة المرور العشوائية" بجانب الـ Session Code كطبقة حماية ثانية.
*   **الهدف:** منع أي اتصال غير مصرح به حتى لو تم تسريب أو تخمين الكود.

## 3. ⚙️ التحكم في جودة الصورة (Quality & Performance Control)
*   **الوصف:** السماح للمستخدمين ذوي الإنترنت الضعيف بتخفيض جودة البث لتقليل التقطيع، ولأصحاب الإنترنت القوي بالحصول على صورة أوضح.
*   **التنفيذ:**
    *   إضافة قائمة منسدلة (Dropdown) في واجهة الـ Host قبل بدء المشاركة لاختيار الدقة.
    *   تعديل الكود الخاص بـ `navigator.mediaDevices.getDisplayMedia` ليشمل خيارات مثل:
        *   **High:** 1080p, 60 FPS
        *   **Medium:** 720p, 30 FPS
        *   **Low:** 480p, 15 FPS
*   **الهدف:** ضمان تجربة سلسة (Smooth UX) تناسب جميع سرعات الإنترنت.

## 4. 📋 مزامنة الحافظة (Clipboard Sync)
*   **الوصف:** إمكانية نسخ نص في جهاز العميل ولصقه تلقائياً في جهاز المضيف والعكس.
*   **التنفيذ:**
    *   مراقبة الـ Clipboard باستخدام أوامر نظام التشغيل أو واجهات المتصفح (Clipboard API).
    *   إرسال التحديثات عبر الـ `WebRTC DataChannel`.
*   **الهدف:** تسهيل نقل الروابط، الأكواد، أو النصوص الصغيرة بين الجهازين دون الحاجة لكتابتها يدوياً.

## 5. 🧑‍💻 تطبيق سطح مكتب متكامل (Desktop App Transformation)
*   **الوصف:** تحويل النظام الحالي المبني على الويب (المتصفح) إلى تطبيق يعتمد عليه كبرنامج حقيقي للتخلص من قيود المتصفح الأمنية والتنبيهات المستمرة ("هذا الموقع يشارك شاشتك").
*   **التنفيذ:**
    *   استخدام إطارات عمل مثل **Tauri** (الأفضل أداءً) أو **Electron.js**.
    *   تضمين خادم الـ Node.js وواجهة الـ React في ملف تنفيذي واحد `.exe` أو `.dmg`.
*   **الهدف:**
    *   السماح للتطبيق بالعمل كخدمة خلفية (Background Service).
    *   الحصول على صلاحيات نظام أعلى (مثل السماح بالتحكم في شاشة قفل النظام (Lock Screen) أو أوامر المسؤول (Admin)).

## 6. 🌐 دعم تعدد العملاء (Multi-Client Broadcasting 1-to-N)
*   **الوصف:** في الوقت الحالي، التطبيق مخصص للاتصال بين جهازين فقط (واحد لواحد). الهدف هو السماح لعدة عملاء بالانضمام لنفس الجلسة والمشاهدة في وقت واحد (مفيدة للشروحات التعليمية أو الاجتماعات).
*   **التنفيذ:**
    *   إعادة تهيئة `server.js` لقبول قائمة من الـ `Clients` بدلاً من عميل واحد، وتوزيع أوامر (Rooms) الـ Socket.io.
    *   تعديل الكود في `HostPanel.jsx` (المضيف) ليقوم بإنشاء خريطة من الاتصالات `Map<SocketId, RTCPeerConnection>` لضرب البث المستمر لكل عميل ينضم بشكل مستقل (Mesh Network).
*   **الهدف:** تحويل التطبيق إلى غرفة اجتماعات/بث تدعم المشاهدين المتعددين مع توزيع أدوار (من لديه حق التحكم ومن لديه المشاهدة فقط).

## 7. 📎 إرسال ومشاركة الملفات (File Sharing)
*   **الوصف:** إمكانية المضيف والعميل مشاركة الملفات ونقلها عبر الاتصال بسهولة وتنزيلها بشكل مباشر.
*   **التنفيذ:** 
    *   تمت إضافة زر رفع (Upload) داخل صندوق الدردشة (ChatPanel).
    *   يتم قراءة الملفات وتحويلها إلى Base64 `DataURL` وإرسالها عبر مسار `socket.io` إلى كل المتواجدين بالجلسة.
    *   يتم إنشاء زر `Download` مخصص في رسالة الدردشة لتحميل الملف المرفق في كل أجهزة العملاء.
*   **الهدف:** تسهيل نقل المستندات والملفات الصغيرة (كأكواد برمجية، ملفات نصية) بيئة العمل.

---

✅ **حالة المشروع الحالية:** الأساسياتتعمل بنجاح عبر **Cloudflare Tunnels** مع **End-to-End Encryption** (100% Secure WebRTC) + الشات (DataChannel) ودعم وضع ملء الشاشة (Fullscreen).


